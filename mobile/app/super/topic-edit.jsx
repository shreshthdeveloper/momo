/**
 * The Central Bank's topic form — and, because a topic cannot exist without a
 * category, the place a central CATEGORY is created. See `app/super/topics.jsx`
 * for why this is the organization console's screen rather than a copy of it.
 */
export { default } from '../admin/topic-edit.jsx';
